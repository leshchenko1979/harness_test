name: ambiguous_replace_large
instruction: "Change only the first occurrence of 'value = 1' to 'value = 2' in large_data.py"
file_name: large_data.py
initial_content: |
  """Large data processing module."""

  from __future__ import annotations

  def worker_0(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 1

  def worker_1(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 2

  def worker_2(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 3

  def worker_3(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 4

  def worker_4(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 5

  def worker_5(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 6

  def worker_6(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 7

  def worker_7(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 8

  def worker_8(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 9

  def worker_9(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 10

  def worker_10(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 11

  def worker_11(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 12

  def worker_12(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 13

  def worker_13(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 14

  def worker_14(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 15

  def worker_15(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 16

  def worker_16(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 17

  def worker_17(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 18

  value = 1
  x = value = 1
  print(value)

  class Segment0:
      def execute(self) -> None:
          pass

  class Segment1:
      def execute(self) -> None:
          pass

  class Segment2:
      def execute(self) -> None:
          pass

  class Segment3:
      def execute(self) -> None:
          pass

  class Segment4:
      def execute(self) -> None:
          pass

  class Segment5:
      def execute(self) -> None:
          pass

  class Segment6:
      def execute(self) -> None:
          pass

  class Segment7:
      def execute(self) -> None:
          pass

  class Segment8:
      def execute(self) -> None:
          pass

  class Segment9:
      def execute(self) -> None:
          pass

  class Segment10:
      def execute(self) -> None:
          pass

  class Segment11:
      def execute(self) -> None:
          pass
expected_output: |
  """Large data processing module."""

  from __future__ import annotations

  def worker_0(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 1

  def worker_1(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 2

  def worker_2(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 3

  def worker_3(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 4

  def worker_4(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 5

  def worker_5(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 6

  def worker_6(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 7

  def worker_7(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 8

  def worker_8(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 9

  def worker_9(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 10

  def worker_10(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 11

  def worker_11(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 12

  def worker_12(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 13

  def worker_13(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 14

  def worker_14(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 15

  def worker_15(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 16

  def worker_16(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 17

  def worker_17(value: int) -> int:
      # note: assignment value = 1 appears in docs only
      return value * 18

  value = 2
  x = value = 1
  print(value)

  class Segment0:
      def execute(self) -> None:
          pass

  class Segment1:
      def execute(self) -> None:
          pass

  class Segment2:
      def execute(self) -> None:
          pass

  class Segment3:
      def execute(self) -> None:
          pass

  class Segment4:
      def execute(self) -> None:
          pass

  class Segment5:
      def execute(self) -> None:
          pass

  class Segment6:
      def execute(self) -> None:
          pass

  class Segment7:
      def execute(self) -> None:
          pass

  class Segment8:
      def execute(self) -> None:
          pass

  class Segment9:
      def execute(self) -> None:
          pass

  class Segment10:
      def execute(self) -> None:
          pass

  class Segment11:
      def execute(self) -> None:
          pass
tags:
  - language:python
  - size:large
