name: whitespace_trap_py_large
instruction: 'Change the greeting string to "Hello, world!" keeping exact indentation (4 spaces) in main().'
file_name: large_app.py
initial_content: |
  """Large application module for hashline whitespace eval."""

  import logging
  from typing import Any

  LOG = logging.getLogger(__name__)
  SETTINGS: dict[str, Any] = {'env': 'dev'}

  def helper_0(x: int) -> int:
      """Padding helper 0."""
      return x + 0

  def helper_1(x: int) -> int:
      """Padding helper 1."""
      return x + 1

  def helper_2(x: int) -> int:
      """Padding helper 2."""
      return x + 2

  def helper_3(x: int) -> int:
      """Padding helper 3."""
      return x + 3

  def helper_4(x: int) -> int:
      """Padding helper 4."""
      return x + 4

  def helper_5(x: int) -> int:
      """Padding helper 5."""
      return x + 5

  def helper_6(x: int) -> int:
      """Padding helper 6."""
      return x + 6

  def helper_7(x: int) -> int:
      """Padding helper 7."""
      return x + 7

  def helper_8(x: int) -> int:
      """Padding helper 8."""
      return x + 8

  def helper_9(x: int) -> int:
      """Padding helper 9."""
      return x + 9

  def helper_10(x: int) -> int:
      """Padding helper 10."""
      return x + 10

  def helper_11(x: int) -> int:
      """Padding helper 11."""
      return x + 11

  def helper_12(x: int) -> int:
      """Padding helper 12."""
      return x + 12

  def helper_13(x: int) -> int:
      """Padding helper 13."""
      return x + 13

  def helper_14(x: int) -> int:
      """Padding helper 14."""
      return x + 14

  def helper_15(x: int) -> int:
      """Padding helper 15."""
      return x + 15

  def helper_16(x: int) -> int:
      """Padding helper 16."""
      return x + 16

  def helper_17(x: int) -> int:
      """Padding helper 17."""
      return x + 17

  def helper_18(x: int) -> int:
      """Padding helper 18."""
      return x + 18

  def helper_19(x: int) -> int:
      """Padding helper 19."""
      return x + 19

  def helper_20(x: int) -> int:
      """Padding helper 20."""
      return x + 20

  def helper_21(x: int) -> int:
      """Padding helper 21."""
      return x + 21

  def bootstrap() -> None:
      SETTINGS['boot'] = True

  def warmup() -> None:
      bootstrap()

  def main() -> None:
      message = "Hi"
      print(message)

  def shutdown() -> None:
      SETTINGS['boot'] = False

  def metrics() -> dict[str, int]:
      return {'calls': 0}

  def helper_0(x: int) -> int:
      """Padding helper 0."""
      return x + 0

  def helper_1(x: int) -> int:
      """Padding helper 1."""
      return x + 1

  def helper_2(x: int) -> int:
      """Padding helper 2."""
      return x + 2

  def helper_3(x: int) -> int:
      """Padding helper 3."""
      return x + 3

  def helper_4(x: int) -> int:
      """Padding helper 4."""
      return x + 4

  def helper_5(x: int) -> int:
      """Padding helper 5."""
      return x + 5

  def helper_6(x: int) -> int:
      """Padding helper 6."""
      return x + 6

  def helper_7(x: int) -> int:
      """Padding helper 7."""
      return x + 7

  if __name__ == "__main__":
      main()
expected_output: |
  """Large application module for hashline whitespace eval."""

  import logging
  from typing import Any

  LOG = logging.getLogger(__name__)
  SETTINGS: dict[str, Any] = {'env': 'dev'}

  def helper_0(x: int) -> int:
      """Padding helper 0."""
      return x + 0

  def helper_1(x: int) -> int:
      """Padding helper 1."""
      return x + 1

  def helper_2(x: int) -> int:
      """Padding helper 2."""
      return x + 2

  def helper_3(x: int) -> int:
      """Padding helper 3."""
      return x + 3

  def helper_4(x: int) -> int:
      """Padding helper 4."""
      return x + 4

  def helper_5(x: int) -> int:
      """Padding helper 5."""
      return x + 5

  def helper_6(x: int) -> int:
      """Padding helper 6."""
      return x + 6

  def helper_7(x: int) -> int:
      """Padding helper 7."""
      return x + 7

  def helper_8(x: int) -> int:
      """Padding helper 8."""
      return x + 8

  def helper_9(x: int) -> int:
      """Padding helper 9."""
      return x + 9

  def helper_10(x: int) -> int:
      """Padding helper 10."""
      return x + 10

  def helper_11(x: int) -> int:
      """Padding helper 11."""
      return x + 11

  def helper_12(x: int) -> int:
      """Padding helper 12."""
      return x + 12

  def helper_13(x: int) -> int:
      """Padding helper 13."""
      return x + 13

  def helper_14(x: int) -> int:
      """Padding helper 14."""
      return x + 14

  def helper_15(x: int) -> int:
      """Padding helper 15."""
      return x + 15

  def helper_16(x: int) -> int:
      """Padding helper 16."""
      return x + 16

  def helper_17(x: int) -> int:
      """Padding helper 17."""
      return x + 17

  def helper_18(x: int) -> int:
      """Padding helper 18."""
      return x + 18

  def helper_19(x: int) -> int:
      """Padding helper 19."""
      return x + 19

  def helper_20(x: int) -> int:
      """Padding helper 20."""
      return x + 20

  def helper_21(x: int) -> int:
      """Padding helper 21."""
      return x + 21

  def bootstrap() -> None:
      SETTINGS['boot'] = True

  def warmup() -> None:
      bootstrap()

  def main() -> None:
      message = "Hello, world!"
      print(message)

  def shutdown() -> None:
      SETTINGS['boot'] = False

  def metrics() -> dict[str, int]:
      return {'calls': 0}

  def helper_0(x: int) -> int:
      """Padding helper 0."""
      return x + 0

  def helper_1(x: int) -> int:
      """Padding helper 1."""
      return x + 1

  def helper_2(x: int) -> int:
      """Padding helper 2."""
      return x + 2

  def helper_3(x: int) -> int:
      """Padding helper 3."""
      return x + 3

  def helper_4(x: int) -> int:
      """Padding helper 4."""
      return x + 4

  def helper_5(x: int) -> int:
      """Padding helper 5."""
      return x + 5

  def helper_6(x: int) -> int:
      """Padding helper 6."""
      return x + 6

  def helper_7(x: int) -> int:
      """Padding helper 7."""
      return x + 7

  if __name__ == "__main__":
      main()
tags:
  - language:python
  - indented
  - size:large
